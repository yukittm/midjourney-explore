# IG publish pipeline runtime deps (stdlib-only domain/validation core; these are for I/O layers).
PyYAML>=6.0      # queue + schedule files
requests>=2.31   # Meta Graph API calls (publish layer)
tzdata>=2024.1   # IANA tz database for zoneinfo (schedule.py) — portable across OS/CI
