# PROGRESS.md

**Updated**: 2026-06-27 | **Project**: midjourney-explore | **Branch**: master [working tree: clean — go-live infra committed+pushed]

> Single progress SSoT for **both Claude and Codex**. Entry rules: `.claude/protocols/progress-management.md`.
> Entry format: `` YYYY-MM-DD [Actor:scope][s:UUID-8] **{🟢|🟡|🔴} title — status / committed `<hash>`** ``

## Current State
Style model **formalized** (validated in-app 2026-06-23). **Render rule = realism scoped to the subject + hard anchors** (photoreal-route kernel: `--style raw` + low `--s` + photo cues + `--no painting/illustration/3d/cgi`); **color = single bold image `--sref`** (`--sw` 150–250; **omit `--sv`** → defaults to sv7, the V8.1 default; set the sref image in the UI, not the prompt body) — the averaging moodboard is retired as the color tool. **The bridge**: name an abstract background as a real drone-photographable phenomenon → one-pass abstract-feel + photoreal figure (R3 validated). **Base + sibling registers** (BASE = R3 Chromatic Wave / *Flowing Color Waves*; SIBLINGS = R1 Color-Block Terrain, R2 Geometric Landform; deferred Pure Color-Field) + a separate **explore lane** (`--c 15–30`). Reference artist = unnamed private `--sref` (name out of system vocabulary). **Automation north-star**: one-pass full-auto (theme → Claude-in-Chrome → IG); user = creative director; **home base = PROVISIONAL Flowing Color Waves** (emerged from selections; firms up via the `--p` loop). **On-style = a REWARD GATE** (subject/anchor realism · saturated chord · one surreal idea + restraint · decisive color-flow-or-light); **realism is SCOPED to the subject + hard anchors** — two recipes (kernel/photoreal-textured + smooth-painterly-wave via profile/high-`--s`/no-raw); the "two-mode" *render* split stays falsified. **Prompt default = rich ~80-word** (detail = the differentiation lever; pair with low `--s`). **§9b Remix-Fidelity lane** = the high-fidelity production method (keeper in BOTH the Remix + Style-Reference slots + `--style raw --s 110 --sw 220` + a rich prompt → variation around a keeper); **§9c (NEW 2026-06-27) = keep-style × change-subject(+composition): `--sref` ONLY (NO Remix — Remix/image-prompt carries the LAYOUT, the cause of subject-collapse; `--sw` only moves color) + subject-first rich prompt + low `--s`; §9b/Remix now reserved for SAME-layout swaps; destination = trained `--p`**. Objective = **own style** (giz = look-anchor); subject **heroless-leaning**; **exact personal palette deferred** (Mariano + own outputs, later). Observation phase for the style. **Publish phase underway:** IG profile locked (**@tim.bankrupt** · Name `Digital Art / AI Visual` · bio `Geometry, Wavelength, Objects 🌊`); growth strategy in `docs/marketing/`; automation = **Option 1 self-owned DIY** (git queue + Python Graph publisher + **GitHub Pages** host; manual-first → scheduled-auto). **Phase-0 pipeline code COMPLETE**, then **upgraded to the converged asset+queue model** (`automation/asset-queue-model.md`; 4-agent design + adversarial verify): **`Post → [ordered Asset]` discriminated model** (image/carousel/reels/story; per-asset alt_text/provenance; `schema_version` + back-compat `from_dict`), a **DERIVED calendar** (`schedule.py` + `schedule.yml`; auto/pinned/hold; `plan.py`), type-dispatched validation (carousel mixed-AR **hard-reject**), and the must-fix holes closed (asset-key↔URL contract, tz-aware/`TypeError`-safe `is_due`, `--id` gate, HEAD probe). **MINIMAL build (image+carousel) implemented, then independently cross-checked (3 review agents → blockers fixed: crash-window double-post STOP, naive-`--now`, id-uniqueness/no-overwrite); 75 tests (72 green + 3 queue I/O skipped locally → CI).** Token verified **60-day** (`debug_token`, ≈2026-08-24) → **regen non-expiring** (not a refresh job). **Pages LIVE** (master/root, built + HTTP-200-verified at `yukittm.github.io/midjourney-explore`; `.nojekyll`). **✅ GO-LIVE WIRING RESOLVED (2026-06-25, via Chrome together):** root cause was the Facebook-Login/`graph.facebook.com` publish path REQUIRES the IG account linked to a FB Page (web-verified) — our IG was a Page-less `instagram-account-v2`. Fix (chose Option A = add Page): linked IG **`tim.bankrupt`** to FB Page **`Tim Bankrupt` (1120164024521237)**, assigned the Page to System User **`publisher`** (content perms), regenerated the token (scopes now incl. `pages_show_list` + `instagram_content_publish` + `pages_read_engagement`). Verified: `/me/accounts` → Page → IG **`17841475449138178`** (= config, CONFIRMED) and `content_publishing_limit` = **100/24h**. **The publish path is GO.** ⚠️ Token is **60-day** (`expires_at` set, ≈2026-08-24; non-expiring not landed) → regen non-expiring before expiry. ⚠️ Real handle = **`tim.bankrupt`** (dot) ≠ marketing-locked `@timbankrupt` → reconcile in `docs/marketing/`. **✅ FIRST LIVE POST PUBLISHED (2026-06-25): media_id `18126888094652044` · permalink https://www.instagram.com/p/DaA4NtuH9Xo/ — the full Phase-0 pipeline (validate → Pages host(200) → create_image_container → media_publish → media_id/permalink write-back → move to `published/`) is PROVEN end-to-end on a real post.** Handle reconciled to `tim.bankrupt`. **⚠️ post #1 `color-window` later DELETED from IG (2026-06-27) — too color-similar to post #2; LIVE grid = 1 post (`long-way-home`).** **Next = steady cadence (more posts + the Escher/exploration lane, applying the `caption` skill's anti-sameness guard in practice); follow-ups: regen the token non-expiring before ≈2026-08-24, decide the Phase-1 scheduled-auto clock.** **Caption method codified (2026-06-27) into the `caption` skill (`.claude/skills/caption/`); `ig-growth-strategy.md` §10 collapsed to a pointer; ⚠️ when the automated/Claude-in-Chrome caption step is built, the anti-sameness scan + tag-vetting must be enforced in code (`igpub/`) — the NL skill is a manual/in-the-loop method, not a headless spec.** FULL build (R2 video/Reels lane, `reconcile.py`, full error-class taxonomy + CLI, cross-post `targets[]`) deferred — see §10. Trending-audio Reels stay manual.

> Solo project (Claude only) — §0 simplification in effect: entries use `[Claude:scope]`, `[s:UUID-8]` omitted. Rule 1 (commit hash on PASS) still applies.

## Roadmap
- [x] Scaffold from template + git init
- [x] Create folder structure (`docs/`, `prompts/`, `moodboards/`, `outputs/`, `automation/`)
- [x] Import Midjourney best-practices research → `docs/research/`
- [x] Analyze reference accounts + set **blend** direction → `docs/style/`
- [x] Import reference image pool → `references/`
- [x] Doc cleanup: de-template contracts + frontmatter convention (`docs/CONVENTIONS.md`)
- [x] Phase 1 start: build & verify the moodboard; set prompt convention + world-view candidates (since consolidated into `docs/style/prompting.md` + `style-definition.md`)
- [x] Validate the V8.1 model in-app (2026-06-23): photoreal kernel + `--sref` color + the bridge + registers R1/R2/R3
- [x] Formalize the model into `docs/style/` + research + automation policy (2026-06-23; 3-agent audit + cross-review)
- [ ] **Promote `style-definition.md` `draft → active`** once a Phase-1 test set holds across R1/R2/R3 + the one-pass path is confirmed end-to-end
- [ ] Lock base prompt templates per register + explore lane → `prompts/`
- [ ] Build Instagram auto-upload pipeline (one-pass path first) → `automation/`

## Entries (newest first)
2026-06-27 [Claude:session] **🟢 session-handoff consumed (repo move) + `caption` skill authored & verified (Full 5-agent → re-verify converged) — in working tree**
  - **Repo-move HANDOFF consumed:** continuity confirmed at the new path (`~/Desktop/dev/midjourney-explore`, working tree clean, 2 published records + 185 candidates matched the snapshot) → `git mv .claude/HANDOFF.md` → `archive/2026-06-27_session-handoff-repo-move.md` (banner + `archive/README.md` index row); removed the 🔔 pointer from this file. No stray refs.
  - **Caption method codified into the project's FIRST skill** `.claude/skills/caption/SKILL.md` (user picked #1). Emits the full publish-text bundle (one-line caption + ≤5 rotated hashtags + literal alt_text) in the @tim.bankrupt voice, with the load-bearing **anti-sameness scan** of the last ~10 `published/*.yml`. Grounded in §10 + the 2 live posts; **`ig-growth-strategy.md` §10 collapsed to a pointer** (T1–T5 migrated in; T5/one-liner = live exemplar, T1–T4 = legacy).
  - **Verified (Tier 3, Full `/verify` 5 agents A–E):** found 1 CRITICAL (hashtags must omit leading `#` — `validate.py` rejects it) + 4 HIGH (over-claimed "spec the automation follows" → scoped to manual/in-the-loop + flagged the scan must be enforced in `igpub/` code for the auto path; one-line/~80/20 default relabeled **PROVISIONAL n=1, revisit after ~10 posts**; carousel/reels post-types added; gate split into text-fixable vs image-level palette-collision STOP; empty-`published/` first-post case) + MEDIUM/LOW (5-slot hashtag mix, content-values-not-yml output mapping, scan sort-key + include deleted records, headless tag-vetting fallback, AI-label, English-by-default, carousel single-AR hard-reject). All folded in → **re-verify round (2 fresh agents) = 0 CRITICAL / 0 HIGH → CONVERGED.**
  - **Hashtag style locked (user feedback):** neutral medium/technique descriptors ONLY (no self-praising/aesthetic-claim tags — `surrealart` retired), **ordered general→specific & consistent**, don't invent tags (`colorfieldart` rejected). Encoded in the skill + `.claude/rules/LESSONS.md`.
  - **Skill re-evolved (`skill-evolve`, user-requested rebuild):** 3-agent critique (general-quality · skill-authoring expert · external research, all converged + Anthropic-docs-grounded) → **split into 2 files**: `SKILL.md` = lean ~120-line method spine (procedure-first, one-rule-one-place, gate=reference checklist, trigger-optimized description), `reference.md` = deferred tables (hashtag library, alt exemplar, worked examples, T1–T5, hard constraints, AI labeling) — Anthropic progressive-disclosure. Fixed a stale `2 niche+2 topic+1 broad` comment, the 5-tag-pool rotation logic (full set differs / partial overlap OK / expand pool), added emoji-set + axis absorbed/stated test + "previous post = most-recent LIVE record" definition. Stability check (fresh agent) = 0 CRITICAL/HIGH, all 13 verbatim constraints intact, cross-refs resolve.
  - **Curation-staging tier added (`outputs/selects/`), after a 3-agent flat eval of the user's "second pool" idea.** Eval verdict (converged): the "raw-dump vs to-post" split ALREADY exists (`outputs/candidates/` vs record-driven `automation/queue/*.yml` + `assets/<id>/`); "folder-as-queue" is incompatible (bypasses caption/alt/crop/schedule/approval-gate/dedupe/anti-sameness → rejected). Real gap = no curated shortlist between the 185-blob dump and the formal yml. → Added `outputs/selects/` = human-only staging (pipeline never reads it); a select **leaves at enqueue, not publish**; no "posted" copy under `outputs/` (posted truth = `published/*.yml` + `assets/<id>/`). Binaries already git-ignored. Deferred (user approved (b)→(a)): an "up-next" read-only view + a `new` scaffold helper (both already FULL-deferred in asset-queue-model.md §10). REJECTED: any 2nd image pool the pipeline reads / folder-as-queue / auto-caption.
  - **`new_post.py` scaffold SHIPPED (3-agent plan cross-review → finalized → implemented).** `automation/new_post.py` + `igpub/scaffold.py`: pick from `outputs/selects/` → scaffold a draft `automation/queue/<id>.yml` + `automation/assets/<id>/01.jpg` via `models.Post`→`save_post`. All preconditions checked before any write (kebab slug, zero-padded date, image exists, **JPEG-by-content — non-JPEG refused, never written under `.jpg`**, no id/asset collision); mid-write failure cleans up the partial asset dir; validate-on-write; optional `--remove-select` (keeps the selects invariant). Caption/alt stay human (caption skill). **`status`/up-next view DEFERRED** — the plan review judged it premature (empty/shallow queue, `ls`-able, model's own YAGNI). Tests **75→92 green**; CLI smoke-clean. Docs synced (asset-queue-model.md §10 `new` FULL→shipped; automation/README; outputs/selects/README enqueue cmd; caption SKILL Output reconciled to the queue draft).
  - **Feed-grid preview UI SHIPPED (user wanted to "see the IG feed arrangement").** `automation/feed_preview.py` + `igpub/feed.py` → generates `automation/feed-preview.html` (gitignored build artifact): 3-col IG-style grid (4:5 tiles), newest top-left, projecting approved → scheduled(desc) → published(desc); `--open` opens the browser. Chosen form = static-generated HTML (no server, per the no-runtime-infra stance; user picked it over a live server). Content = published + queue(scheduled/approved). **Added first-class `Post.deleted_from_ig` field** (additive; replaces the manual marker) so the preview EXCLUDES deleted posts — caught that `color-window` (deleted) would otherwise mis-render as live; now the grid correctly shows 1 tile (`long-way-home`). Tests **92→103 green**.
  - **"Feed studio" (drag-rearrange UI) planned → 2-agent verify → re-scoped → candidate contact-sheet SHIPPED instead.** User wanted a UI to grab/rearrange upcoming posts on the grid + an idea to pull candidates→selects in-UI. A light 2-agent verify converged: the drag-reorder server is **premature** — the upcoming queue is empty (selects/ empty, queue/ = example; only 1 live post), so there's nothing to arrange (same "shallow-queue" reason `status` was deferred); and `priority`-only reorder silently no-ops on scheduled/pinned posts. The real daily friction = 185 raw candidates browsed in Finder. candidates→selects file-move was rejected (raw PNG breaks the selects=cropped-JPEG contract → `new_post` refuses it). **Built the high-value, zero-infra piece: `automation/contact_sheet.py` + `igpub/contact.py`** → static `outputs/candidates/contact-sheet.html` (gitignored): all 185 candidates grouped by subject (27 groups, biggest first), click=full, hover=filename — triage at a glance to pick what to crop into selects/. Tests **103→111**. **Deferred (build when a real backlog exists):** the drag-rearrange Studio (local Flask), and when built do **approve + reorder together** (full loop, not reorder-only) with the fix that only AUTO+APPROVED tiles are reorderable + the API returns the actual `assign_slots` result.
  - **Follow-ups (unchanged):** Area B (architecture) exploration; user triages `outputs/candidates/` via `contact_sheet.py` → crops keepers into `outputs/selects/` → `new_post.py` → `feed_preview.py`; the drag-rearrange Studio once a backlog exists; regen token non-expiring before ≈2026-08-24; CLAUDE.md↔AGENTS.md Quick-mode drift (user decision); the auto-path code-enforced anti-sameness check.

2026-06-27 [Claude:content] **🟡 post #1 (color-window) DELETED from IG by user — record aligned — in working tree**
  - User deleted the 1st live post (`2026-06-25_color-window`, media_id 18126888094652044) in the IG app — its color read too similar to post #2. **Live grid now = 1 post** (`long-way-home`, https://www.instagram.com/p/DaDhalyn6iM/).
  - Record aligned: `automation/published/2026-06-25_color-window.yml` keeps `status: published` (the publish DID happen — audit trail) + new `deleted_from_ig: true` / `deleted_at: 2026-06-27` / `deleted_reason` marker. No `deleted` enum status yet (`Status.deleted` = the deferred §7 seam; a marker avoids a code change for one manual deletion — offered to implement the first-class seam if wanted).
  - Signal: 2 posts in a row read color-similar → **vary the palette/subject on upcoming pieces** (the anti-sameness concern, now live).

2026-06-27 [Claude:content] **🟢 2nd live post + cosmic area DONE + keep-style×change-subject method confirmed & documented — in working tree**
  - **2nd live IG post PUBLISHED:** `2026-06-27_long-way-home` (villa + flowing-color path) → media_id **18091953614622428**, permalink https://www.instagram.com/p/DaDhalyn6iM/. Caption "Home, the long way." (one-line, casual, axis absorbed ~80/20 — §10 anti-sameness vs post #1); 5 rotated tags; alt set. Same pipeline (PIL crop→exact 4:5 1968×2460 JPEG → committed under `automation/assets/` → Pages → `publish.py --id` → media-id/permalink write-back → moved to `published/`).
  - **Exploration: Area C (cosmic) DONE; Area A (cactus) PARKED (unfinished); next = B (architecture).** Cosmic landed as a **lone figure on a gravity-defying flowing-color ribbon** — the "reality-defying constructs" parked lane → **VALIDATED** (trigger = remove the floor/landscape → the form floats in a void and can twist; figures ride one continuous surface at multiple orientations). Cactus parked: macro "plant = wave" works on sculptural forms, but object↔virtual on a solid spiny subject is hard.
  - **Method CONFIRMED & documented** (multi-agent research + cross-review, official-doc-grounded): **keep-style × change-subject = `--sref` ONLY + subject-first rich prompt + LOW `--s`** — Remix/image-prompt drags COMPOSITION (the real cause of the cactus/cosmic "collapse"); raising `--sw` only moves color. Wrote **`prompting.md` §9c** (canonical change-subject+composition method), reframed **§9b** to same-layout swaps, added **§5c scale↔abstraction**, **§6b Figures** (style figures intentionally to avoid generic/anonymous — specific aesthetic is a per-series choice, not a fixed rule; de-clone multiples by enumerating distinct individuals + "different", shared wardrobe once; `--chaos`/`--weird`/`--no` do NOT de-clone; small multiples malform → prefer 1 / Vary-Region / curate), reinforced `--no` single-token rule + ~74-token influence cap. Updated `exploration-map.md` (area statuses + reality-defying lane → validated). Detailed param/craft research saved by agents under `.claude/agent-memory/researcher/reference_mj-*.md`.
  - **Follow-ups:** (1) build a **caption-writing SKILL** (codify the §10 evolution: one line, ~80/20 axis-absorb, casual register, anti-sameness, approach-based templates); (2) **Area B (architecture)** exploration; (3) regen the token non-expiring before ≈2026-08-24; (4) Phase-1 scheduled-auto clock.

2026-06-26 [Claude:automation] **🟢 FIRST LIVE IG POST PUBLISHED — full Phase-0 pipeline proven end-to-end — committed**
  - Published `2026-06-25_color-window` (the geometric-portal flowing-color image) to **@tim.bankrupt** via `python automation/publish.py --id`. Result: **media_id `18126888094652044`**, **permalink https://www.instagram.com/p/DaA4NtuH9Xo/**, published 1 / failed 0.
  - Flow proven on a REAL post: PNG→JPEG (sips, center-cropped to exact 4:5 1968×2460) → committed under `automation/assets/` → **GitHub Pages (HTTP-200 verified)** → `create_image_container` → `media_publish` → media_id+permalink recorded → queue file **moved to `automation/published/`** (git = audit trail). The asset-key↔URL contract, idempotency, write-back, and host all worked live. Token (60-day) + `ig_user_id 17841475449138178` confirmed working.
  - Caption = the §10 voice exemplar ("A quiet valley… / Geometry, holding a wavelength 🌊"); hashtags `geometry/wavelength/digitalart/generativeart/midjourneyart`; alt text set.
  - Deps: installed PyYAML locally (sandbox-off, like the .env write) to run `publish.py`; `requests` present; ran in-sandbox (Python network to graph.facebook.com OK).
  - **Next:** steady cadence (more posts + Escher/exploration lane; apply §10 caption anti-sameness); follow-ups — regen the token non-expiring before ≈2026-08-24, choose the Phase-1 scheduled-auto clock.

2026-06-25 [Claude:automation] **🟢 GO-LIVE wiring RESOLVED — IG↔Page linked + Page→System User + token regen; publish path verified GO — in working tree**
  - Fixed the holistic-review blocker (token couldn't reach the IG account) together via Claude-in-Chrome. **Root cause (web-verified):** the pipeline's Facebook-Login/`graph.facebook.com` publish path REQUIRES the IG account linked to a FB Page; our IG was a Page-less `instagram-account-v2`. Chose **Option A (add Page)** over Instagram-Login (which would need code rework + 60-day refresh).
  - Steps (Meta Business Settings): linked IG **`tim.bankrupt`** → existing FB Page **`Tim Bankrupt` (1120164024521237)**; assigned the Page to System User **`publisher`** with content perms; user regenerated the System User token (scopes: `pages_show_list`/`instagram_basic`/`instagram_content_publish`/`pages_read_engagement`/`instagram_manage_*`). Saved to `automation/.env` (gitignored, chmod 600).
  - **Verified GO:** `/me/accounts` → Page → `instagram_business_account` id **17841475449138178** (= `config.yml`, so the id was right all along) username `tim.bankrupt`; `content_publishing_limit` = **100/24h**. config.yml comment corrected to `@tim.bankrupt`.
  - **Two follow-ups:** (1) token is **60-day** (≈2026-08-24) → regen non-expiring before expiry (also rotates the value that got pasted in chat); (2) real handle `tim.bankrupt` (dot) ≠ marketing `@timbankrupt` → reconcile `docs/marketing/`.
  - Process notes (lessons): accidentally auto-triggered a token-gen while driving the dialog (value briefly shown) → discarded/superseded; user pasted the real token in chat despite the no-paste rule → saved to `.env` via Write (pbpaste returned empty), never committed (secret-scan clean). Browser-driving was slow (1 screenshot/click) → switch to batching + hand credential steps to the user.
  - **Next = first live test post** (1 graded JPEG under `automation/assets/<id>/` + caption/alt → `validate`→`plan`→`publish` with a final confirm before the public post).

2026-06-25 [Claude:repo] **🟢 clean-state pass — must-fix doc↔code gaps + cruft removed; CI secret-scan added — in working tree**
  - User: reach a completely clean state (fix all must-fix · modernize structure · archive legacy · zero cruft) BEFORE the Meta-wiring switch. Inventory + actions:
  - **doc↔code lie fixed:** `ig-publish-pipeline.md` promised a `gitleaks` guard that never existed → implemented a **dependency-free CI secret-scan** (`validate.yml` job: Meta token-pattern `EAA…` + `.env`/`config.yml`-not-tracked; verified it passes locally) and aligned the doc.
  - **cruft removed:** empty `agent-memory/security-auditor/` (left by the user-rejected security agent); committed the dangling agent-memory (researcher rate-limit note →100/24h, code-reviewer idempotency-model note).
  - **honesty touches:** `config.yml` ig_user_id comment `@tim.bankrupt`→locked `@timbankrupt` + RE-CONFIRM flag (ties to the go-live blocker); `ig-publish-pipeline.md` "image paths"→"asset keys" + Secrets section now describes the real env-var/`.env` setup; `outputs/README` cross-refs the `automation/assets/` publish-subset.
  - **Structure verdict** (this session's Lens-2 + the `41d345d` audit): **sound, right-sized — no restructure/archive needed**; the two design docs are complementary (not redundant), the folder tree is clean. Deliberately left craft choices (`--c 10`) and did not over-merge docs (balance: no 重箱の隅). **Next = the Meta-wiring switch (assign Page→System User, regen token, reconfirm ig_user_id) → first post.**

2026-06-25 [Claude:review] **🟡 holistic 4-agent project review (balanced: load-bearing, not nitpicks) — project healthy; 1 go-live BLOCKER found + hygiene fixed — in working tree**
  - 4 parallel review agents (coherence/SSoT · architecture/extensibility · strategy-coherence · completeness/risk) over the WHOLE project, balance = rework/extensibility-risk not 重箱の隅. **Verdict: healthy — no one-way doors, code already vetted, no over-build; structure right-sized.**
  - **★CRITICAL (found by verification): go-live token wiring is broken.** Saved System User token is valid (graph.facebook.com SYSTEM_USER) but `/me/accounts`=empty + `ig_user_id 17841475449138178` inaccessible + `graph.instagram.com` rejects it → the Page/IG asset isn't assigned to the System User. **Blocks the first post.** Fix = assign Page to System User (Business Settings) + regen token (pages_show_list/business_management) + reconfirm ig_user_id. (Earlier "go-live setup DONE" was over-optimistic — the publish path was never exercised.)
  - **★STRATEGIC (Lens3+4 converged): the pipeline is ahead of the content engine.** Cold-start growth = Reels (strategy's explicit claim), but Reels can't auto-publish (API limit) AND have no content-production method in the style docs; style is still PROVISIONAL/draft (only R3 validated); no 9-12 runway. The publish plumbing is done+solid, but the goal's binding constraint is **upstream** (firm the style → build a runway → decide the Reels reality). A sequencing insight, not a defect — creative-director call.
  - **Cheap fixes applied (judgment-free):** `timbankrupt.github.io`→`yukittm.github.io` (hosting.py docstring + test_hosting.py, stale); `ig-publish-pipeline.md` rate `~25`→`~100/24h` (2×, was 4× under); dropped unimplemented `skip_dates[]` from asset-queue-model.md §3. 75 tests still green.
  - **Flagged for user decision (deferred, NOT over-built now):** public-repo provenance = prompt/sref world-readable (intentional give-away vs protect; gitleaks guard is doc-only/unimplemented); a 1-page generation→queue handoff contract (cheap insurance for the future Claude-in-Chrome path); clock choice VPS-vs-Lambda (Phase-1); cross-image sameness gate (the gate is per-image only — the user's #1 fear is unenforced in the auto loop) → future. Handle `@timbankrupt` vs config-comment `@tim.bankrupt` unresolvable until the Meta wiring is fixed (then `/me/accounts` reveals the real username).

2026-06-25 [Claude:automation] **🟢 asset+queue management model — converged (4-agent) + adversarially verified + 3-agent code review + MINIMAL build — PASS / committed `76ee4b7`**
  - User: design how post media + the posting queue are managed — near-best, hole-free, high-extensibility; settle WHERE in the structure + ALL IG media types (image/video/Reels/carousel/multi-photo) BEFORE building (rework later is costly). Process: defined 6 perspectives → **4 background agents** (A IG-media data-model · B storage/host · C scheduling/ops · D extensibility+adversarial-holes) → converged into `automation/asset-queue-model.md` → **1 adversarial verifier** (1 CRITICAL + 6 HIGH, all folded in).
  - **Design (叩き台, verified):** `Post → [ordered Asset]` discriminated by `media_type` — ONE schema covers every IG type, additive (enum + rule + call-mapping). Per-asset `alt_text`/`user_tags`/`provenance`; `schema_version` + back-compat `from_dict` (legacy `images:`→assets; legacy `approved`+publish_at→`scheduled`). **Host split:** images→**main-repo Pages**, video→**R2** (retire the separate assets repo; fixes the prod-404 key↔URL contract — `config.yml pages_base` was pointing at the wrong repo). **Derived calendar:** queue=SSoT, `schedule.yml` cadence + `schedule_mode` (auto/pinned/hold), pure `assign_slots`. Holes closed: tz-aware/`TypeError`-safe `is_due`, `--id` gate (refuse draft/hold/published), container-id-before-publish + run-lock, HEAD reachability probe, carousel mixed-AR **hard-reject**, live rate-limit.
  - **CRITICAL fixed (evidence over doc):** verifier flagged my "token is 60-day" decision as a false premise (Meta docs: System User token non-expiring by default). I ran `debug_token` on the **SAVED** token → it IS **60-day** (`expires_at`=issued+5,184,000s ≈2026-08-24; my original read was right; non-expiring is an *opt-out* option). → decision = **regenerate non-expiring** (NOT a refresh job) + `doctor` warns <14d. (LESSON: verify the artifact, not the general doc.)
  - **MINIMAL build implemented (image+carousel; no video yet):** `igpub/models.py` (Asset/Provenance/`Post.assets`/STORY/`schema_version`/`schedule_mode`/result-envelope + back-compat) · `igpub/schedule.py` (NEW pure assigner) · `igpub/validate.py` (type-dispatched + AR hard-reject) · `igpub/publish.py` (`is_due`→SCHEDULED + hold/reels guards + HEAD probe) · `igpub/graph.py` (per-asset `alt_text`) · `plan.py` (NEW CLI) · `publish.py` runner (`--id` gate + HEAD) · `schedule.yml` + `_EXAMPLE.yml`(→`assets:`) + `config.yml`/`config.example.yml`/`.gitignore`(video)/`requirements.txt`(tzdata) + doc-sync (`ig-publish-pipeline.md`). **Suite = 57 tests (54 green + 3 queue I/O skipped in-sandbox → run in CI).** FULL build (R2 video lane + `upload_video`, `reconcile.py`, full error-class taxonomy + rest of CLI, cross-post `targets[]`, insights write-back) deferred — `asset-queue-model.md` §10.
  - **Independent code review (3 agents: correctness · design-conformance · test-adequacy) → blockers fixed:** (1) **crash-window double-post** — `container_id` now saved BEFORE publish + a stale-`publishing`-with-container row is REFUSED on re-run (manual reconcile, never a blind re-post); (2) **naive `--now`** coerced to UTC in both runners; (3) **doc-vs-code lie** — `id` uniqueness (`duplicate_ids` in `validate.py`) + `move_to_published` **no-overwrite** were claimed-but-missing → now implemented; `run-lock`/claim-via-rename honestly demoted to FULL; cap documented as ±24h-approx backstopped by the publish-time limit pre-check. Added tests: `--id` gate (`id_publishable`), container-ERROR→FAILED, stale-`publishing` STOP, min_gap/24h-cap, `publishing_limit` Meta-shape parse, no-overwrite, duplicate-ids. Made `queue.py`/`config.py` lazy-import yaml (consistency). **Suite 57→75; runners smoke-import clean; `validate.py` runs e2e.**
  - **Next:** first live test post (1 real graded JPEG + a final confirm before the public post) + (before ≈2026-08-24) regen the token non-expiring. Committed `76ee4b7` (reviewed; 75 tests), pushed; **GitHub Pages now LIVE** (master/root, built + HTTP-200-verified, `.nojekyll`; `b6a77a5`).

2026-06-25 [Claude:docs] **🟢 doc-freshness pass (2-agent audit) — R2→Pages drift + locked-profile + SoT congruence fixed — PASS / committed `8aab6c2`**
  - 2 independent doc-audit agents (host-change/SoT · xrefs/conventions) after the session's many direction-changes → repo mostly clean; fixed: `automation/README.md` (stale "Cloudflare R2 host" + "Code pending build" + date→2026-06-25), `ig-publish-pipeline.md` (2 residual "R2" schema/flow tags → Pages / repo-relative paths), `ig-growth-strategy.md` (`draft`→`active` + "handle/name TBD"→locked `@timbankrupt` + date), `docs/style/README.md` date→2026-06-24, and made the **§Source-of-truth lists congruent** across CONVENTIONS/CLAUDE/AGENTS (added `/archive/` to CLAUDE/AGENTS, `CONVENTIONS.md` to CONVENTIONS). Historical R2 PROGRESS entries left as the append-only log (accurate as-of-their-time). All wikilinks/relative paths resolve; no live `moodboards/` refs.

2026-06-25 [Claude:automation] **🟢 Phase-0 final slice — Graph publish client + orchestration + publish CLI (mock-tested); pipeline code COMPLETE — PASS / committed `3494515`**
  - `igpub/graph.py` (GraphClient: image/carousel containers · container_status · media_publish · permalink · content_publishing_limit; token never logged; `requests` lazy-imported → mock-testable). `igpub/publish.py` (`publish_one` over injected deps: idempotent media-id skip → claim PUBLISHING → create → **bounded poll** (ERROR/EXPIRED/timeout=fail) → publish → record media-id → PUBLISHED + move; FAILED on error; `is_due`). `automation/publish.py` (CLI: `--id` or due-now · token-from-env · 25/24h pre-check · healthcheck ping) + `tests/test_publish.py`. **Suite = 31 tests (29 green + 2 queue skipped in-sandbox → run in CI). Phase-0 pipeline code COMPLETE.**
  - **Go-live (user-side):** create Meta **System User non-expiring token** → env `IG_SYSTEM_USER_TOKEN`; **enable GitHub Pages** on the public repo; copy `automation/config.example.yml`→`config.yml` with `ig_user_id` + `host.pages_base`. Then Phase-0 test: `python automation/publish.py --id <id>`.
  - **In flight:** a 2-agent **doc-freshness audit** (post direction-changes) is running — fixes to be applied next.

2026-06-25 [Claude:automation] **🟢 Phase-0 slice 2 — host=GitHub Pages locked + queue/config/hosting + validate CLI + CI — PASS / committed `3907924`**
  - **Host decided = GitHub Pages on the public main repo** ($0, no domain, no extra account, git-versioned provenance; swappable via the host adapter → R2/Cloudinary later, esp. for video/Reels). Design doc updated. **Reels deferred** (user will likely add later). Confirmed the design is UI-/swap-flexible: headless core + adapters + data-contract queue → changing host/clock/UI is one localized piece, not a rewrite.
  - Landed: `igpub/hosting.py` (`ImageHost` adapter + `GitHubPagesHost` URL-mapper + `build_host`), `igpub/queue.py` (YAML load/list/save/move-to-published), `igpub/config.py` (config + token-from-env), `automation/validate.py` (queue-validator CLI, non-zero on failure), `config.example.yml`, `queue/_EXAMPLE.yml`, `assets/`+`published/` dirs, `.github/workflows/validate.yml` (CI: validate + tests). Tests: **24 total — 22 green + 2 queue tests skipped in-sandbox (no PyYAML) → run in CI**.
  - **Next (final slice):** Graph publish client `igpub/graph.py` (container → bounded poll → media_publish → media-id idempotency → `content_publishing_limit` pre-check → healthcheck) + `publish.py` orchestration + CLI. Then user-side prereqs: Meta System User token + enable GitHub Pages.

2026-06-25 [Claude:automation] **🟢 Phase-0 pipeline core foundation — models + validation, tested — PASS / committed `325e2aa`**
  - First code of the IG pipeline (Option 1 DIY). Landed the **headless core** `automation/igpub/`: `models.py` (Post / Status / MediaType + (de)serialization), `imaging.py` (stdlib JPEG dimension read + the 4:5..1.91:1 aspect rule, no Pillow), `validate.py` (commit-time checks: caption ≤2200, ≤30 hashtags, carousel 2..10, image=1, JPEG-only, aspect) + `tests/test_validate.py`. **17 unit tests green** (stdlib only — no auth/network). Verification command set in CLAUDE.md/AGENTS.md (`PYTHONPATH=automation python3 -m unittest discover -s automation/tests`). UI-ready layering per the design doc §Extensibility.
  - **Next slice:** `queue.py`/`config.py` (YAML I/O), the Graph publish layer + `publish.py`/`validate.py` CLIs, the CI workflow, `_EXAMPLE.yml`. **Host note:** R2 custom domain is NOT required (r2.dev rate-limited/dev-only but irrelevant at ~1/day; host is adapter-decoupled) → can start domain-free, decide at Phase 1.

2026-06-25 [Claude:repo] **🟢 project structure cleanup (3-agent audit → converged) + pipeline Extensibility/UI-ready design — PASS / committed `41d345d`**
  - User: keep the whole tree optimal/clean/minimal-redundant, and design the pipeline UI-extensible. 3 independent structure-audit agents (minimalist / phase-SSoT / maintainability) converged: repo already healthy → a **conformance punch-list, not a restructure**. Applied: added `docs/marketing/README.md`, `references/README.md`, `scripts/README.md`; updated §Source-of-truth in `docs/CONVENTIONS.md` + `CLAUDE.md`/`AGENTS.md` (added `docs/marketing/` + `automation/`); fixed a dangling wikilink (design-reviewer memory) + a stale `outputs/README.md` sidecar field (moodboard→`--sref`); documented the dated-bundle archive-banner exemption + the scripts↔automation boundary; committed the untracked researcher IG-publishing reference. **Archived `moodboards/`** (retired color tool, empty folder, no live refs) → `archive/2026-06-25_moodboards-retired.md` (move-not-delete); future `--p` notes → `docs/style/` or a new `profiles/`.
  - Added an **Extensibility & UI-readiness** section to `automation/ig-publish-pipeline.md`: headless core (`igpub/`) behind a stable internal API, thin CLI now, a UI bolts on later with zero core changes; side-effects behind adapters; config-driven. Standing principle = clean / flexible / low-maintenance.

2026-06-25 [Claude:automation] **🟢 decide IG publish architecture — Option 1 self-owned DIY (design doc) — PASS / committed `c109adc`**
  - Pivot to designing the operation (IG connection · image↔caption · auto-trigger · web-app?). **Decisions:** publish trigger = **fully scheduled auto-publish from a human-APPROVED queue**; account = own IG Business+FB → **no App Review (Dev mode), non-expiring System User token**; **chose Option 1 self-owned DIY** (git-file queue with approval=commit; ~100-line Python Graph 2-step publisher; **Cloudflare R2 + custom domain** as the fixed image host; secrets in Keychain/host store) over the no-code Notion+Make path — per the user's distrust of connectors as runtime infra. Phase 0 manual (publish stays by-hand) → Phase 1 scheduled; **clock (Hetzner VPS cron ~$4.6/mo vs AWS Lambda+EventBridge) decided at build time** (NOT GitHub Actions cron — delays + 60-day auto-disable). Design doc: `automation/ig-publish-pipeline.md`.
  - Process: 4 independent agents (Graph-API research + 3 architecture designs A/B/C + a first-principles stack study) → converged → 1 adversarial cross-check. Folded-in corrections: media-ID idempotency key, bounded container-poll (handle ERROR/EXPIRED), `content_publishing_limit` pre-check, commit-time validation, healthcheck dead-man's-switch. **Key caveat:** trending/licensed-audio Reels can't be auto-published via ANY API → stay manual (native app); the pipeline carries images/carousels (+ silent Reels).
  - Next: Phase-0 build (scaffold + `publish.py` + `validate.py` + CI). User-side prereqs: Meta System User token + Cloudflare R2 bucket/custom domain (needs a domain ~$10/yr).

2026-06-24 [Claude:marketing] **🟢 research-grounded IG growth strategy + bio/captions; PFP/icon standalone menu — PASS / committed `3b54fca`**
  - Pivot to the **publish phase**. IG = Business/Creator + FB-linked (official Graph API viable later; **publishing MANUAL for now**, no paid APIs, creative-director-led). Built a research-grounded growth strategy: 3 parallel researchers (algorithm/growth · AI-art big-account playbook = giz/Mariano · bio+caption craft) → synthesized `docs/marketing/ig-growth-strategy.md` → independent design-reviewer pass (no CRITICAL; applied HIGH/MED fixes: Google-index scope, "velocity"/"7s" flag corrections, **added Phase 0 cold-start**, de-baited captions, deferred prints-in-bio to Phase 2). Confidence-flagged ✅Meta-confirmed / 🟡analyst-directional.
  - Key model: IG = search/discovery engine; **Reels = the only cold-reach surface at 0 followers**; **signature-consistency = #1 growth lever**; keyword-SEO (name/bio/caption/alt) replaces hashtags (≤5); self-label AI. Handle = **Tim Bankrupt** (EN-primary). First 3 moves: 9-12-piece runway → positioning(bio B, no-sell) → Reels-first + Trial Reels.
  - Also formalized the **PFP/icon line-art** work as a **standalone menu** (`prompts/icon-pfp/README.md`; adopted V1 = no-theme abstract wave circle). Flat-graphic params kept **LOCAL — NOT merged into core style** (PROGRESS Context guard). Finding: the `--sref` pick dominates from-scratch output.

2026-06-23 [Claude:method] **🟢 capture the Remix-Fidelity production lane (prompting.md §9b) — PASS / committed `3e8edfa`**
  - In-app, the highest source-fidelity results (dancers / surfer sets) came from: keeper in **BOTH** the Image-Prompt(Remix) **and** Style Reference slots + `--style raw --s 110 --sw 220 --no painting, illustration, 3d render, cgi` (profile/moodboard OFF) + a **rich** prompt. = high look-fidelity (source in both slots) + a **differentiated subject** (raw + low-`--s` + rich prompt). Recorded as **§9b Remix-Fidelity lane**: variation-around-a-keeper — rewrite the prompt text → new subject, ~same texture/color/composition. Validates the photoreal/bridge route + the rich-prompt principle (high `--s`/smooth-wave was lower-fidelity). Caveat: inherits the source's composition (big departures → from-scratch §5/§7 or explore §9).

2026-06-23 [Claude:style] **🟢 formalize the personal style from selections — base = Flowing Color Waves (PROVISIONAL) + render-scope + base/sibling + gate v2 — PASS / committed `eb0a642`**
  - Reviewed the user's curated keepers → **PROVISIONAL home base = Flowing Color Waves (R3)**; registers re-slotted **base (R3) + siblings (R1/R2)**. **Render rule rescoped:** realism is scoped to the **subject + hard anchors** (two on-style recipes — kernel/photoreal-textured + smooth-painterly-wave via profile/high-`--s`/no-raw, subject clean/legible there); the "two-mode" *render* split stays falsified (this is a SCOPE statement, not a mode). **Reward gate rewritten as positive tests** (GATE④ passes a clean legible color-flow, fails a muddy blur). **3-layer variation** (within-base / sibling / explore-lane); proportion emerges from selections. Dropped "white sphere" as a featured motif (the sphere *placement* was disliked; geometric objects stay a normal option — no demotion note, per user).
  - Process: design-reviewer ×2 (caught that the first draft re-opened drift + inverted the axiom → tightened to scope + positive-gate). Edited style-definition / prompting / README / reference-accounts. PROVISIONAL (not "decided") per the deferral clause + the over-fit LESSON. Next: run the variation batch; promote home base to "decided" once selection history confirms it.

2026-06-23 [Claude:research] **🟢 4-agent web re-verification of the MJ doc — corrected the `--sv` rationale; all else confirmed current — PASS / committed `c1a805f`**
  - User asked to web-verify the whole MJ research doc is current + "absolutely not wrong" after the `--sv 7` error. 4 background agents (model/render · style-ref · refs/automation · recipe-contamination audit).
  - **Key correction:** the first-pass `--sv` *rationale* was itself wrong. Truth (4 sources): `--sv 7` is the V8.1 **default** for image srefs + moodboards and is **`--hd`-compatible**; the live error was an explicit-`--sv 7` / numeric-code quirk, NOT a ban; numeric CODES need sv4/sv6. The *action* (omit `--sv` for image-URL srefs) was already right. Also corrected: "moodboards ignore `--sv`" (wrong — they default to sv7); `--sw` bands → documented heuristic; `--exp` 0–100 (dropped "verify"); added `--q` **unsupported on V8.1**.
  - **Confirmed current (no change):** V8.1 still the default (no V8.2/V9); **no V8-native omni / editor / official API** → the one-pass + manual-compositing + ToS-risk policy holds; `--style raw` / `--s` (50–150 photoreal) / `--no` / `--iw 0–3` / `--hd` / `--seed` / `--c` all hold. Contamination audit: **no residual `--sv 7`**; recipe `--s 90–120` is safe (inside 50–150).
  - Edited research doc (§1, §2 table, §3, §6, §7), style-definition, prompting, LESSONS, prompts/README. Remaining verify-in-app residuals: exact V8.1 `--iw` max, exact `--sw` bands, the explicit-`--sv 7` error trigger.

2026-06-23 [Claude:fix] **🟢 remove `--sv 7` from all recipes (V8.1 rejects it) — PASS / committed `832251d`**
  - Live submit error *"Unsupported Style Reference version 7 for --version 8.1"* traced to `--sv 7` baked into every prompt recipe (from a research-doc claim flagged "verify in-app"). **Fix: for an image `--sref` on V8.1, OMIT `--sv`** (model default; `--sv 6` incompatible with `--hd`, `--sv 4` forces V7). Scrubbed `--sv 7` from style-definition / prompting / automation / prompts; corrected the research-doc `--sv` row to in-app fact. LESSONS updated (verify load-bearing params in-app before baking into recipes). Re-issued the test prompts without `--sv 7`.

2026-06-23 [Claude:style] **🟢 lock the style signature (full-evidence) + reward gate + decisive-light lever — PASS / committed `2052e9c`**
  - Re-grounded the giz signature on the **full 11-image curated set** (3 independent reads + an adversarial check) after catching a **2-image over-generalization** (LESSONS updated). **Falsified** the proposed "two-mode flat-graphic / warm-photographic split" — all 11 are photoreal; "graphic" = composition + hard light. **ONE photoreal render mode kept.**
  - **Added the on-style REWARD GATE** (① photoreal · ② saturated red/green/blue chord · ③ one surreal idea + restraint · ④ **decisive light**) as the anti-drift objective ("on-style" = a checklist, not a vibe), and the **decisive-light lever** (hard-graphic ⇄ golden-warm). Surgical additions; kernel/registers/bridge/explore/one-pass all kept (no rewrite).
  - **User decisions locked:** objective = own style (giz = look-anchor; curated 11 = chosen direction, not all-of-giz); home base = deferred; subject = heroless-leaning/balanced; palette = giz-bold-saturation now, **exact personal palette deferred** (Mariano + own outputs, later); removed "soccer"/specific-subject words from the style docs; "Claude-alone" bounded honestly (prompt-gen + LUT automatable; sref-upload/browser/`--p`/selection/publish human-assisted).
  - Process: 3-agent build + adversarial hole-find + reward/decisions, converged; the adversarial pass caught the two-mode error **before** any doc write (= the no-rework the user asked for). Edited style-definition / prompting / reference-accounts / automation. Next = the **reward-gate validation test** (R1/R2 + both lights, fresh subjects) → promote `draft → active`.

2026-06-23 [Claude:docs] **🟢 formalize the `/archive/` convention (durable / memory-reset-proof) — PASS / committed `e14fdde`**
  - Made `/archive/` the single, self-documenting home for legacy. Added an **Archiving** section to `docs/CONVENTIONS.md` (move-not-delete procedure + `archived` status + the banner format); rebuilt `archive/README.md` as an index recording **archived-date · why · superseded-by** per item + a "how to add" procedure; added a self-identifying `⚠️ ARCHIVED YYYY-MM-DD — why → superseded-by` banner to each archived doc (and fixed the V7 bundle banner's stale path).
  - Goal: after a memory reset, **what / when / why / replaced-by** is recoverable from the files alone (banner) + `/archive/README.md` (index), without prior context.

2026-06-23 [Claude:docs] **🟢 introduce `/archive/` + relocate V7-era legacy out of the live tree — PASS / committed `309d582`**
  - Created top-level **`archive/`** (README + supersede table). `git mv`'d the V7-era research bundle `2026-05-11_midjourney-prompt-best-practices/` (out of `docs/research/`) and the V7-era moodboard-automation idea (out of `automation/`) into it; removed the vestigial empty `agent-memory/design-reviewer/` dir.
  - Updated all pointers (research doc frontmatter + body; `docs/research/README.md` archive section, "8 files"→"7"; `automation/README.md`). `docs/research/` is now current-only (V8.1 doc + README); `automation/` is README-only. No broken refs.
  - Added a LESSONS entry: move superseded docs into `/archive/` (don't delete, don't leave in place) to keep the live tree clean.

2026-06-23 [Claude:style] **🟢 formalize validated V8.1 style model — photoreal invariant + 3 background registers + the bridge + automation policy — PASS / committed `ef27769`**
  - In-app validation (2026-06-23) replaced the moodboard/2-dial model. New SSoT model: **foreground INVARIANT = photoreal** (kernel = `--style raw` + low `--s ~90–120` + photo cues + `--no painting, illustration, 3d render, cgi`); **color = single-image `--sref`** (`--sw` 150–250; omit `--sv`), push color via `--sw` not `--s` — averaging moodboard retired as the color tool.
  - **THE BRIDGE**: ground abstract/flowing backgrounds in REAL drone-photographable phenomena (crop/tulip bands, painted-desert strata, terraced/contour fields, salt ponds, aerial bands) → one raw pass yields abstract-feel + real figure. Breakpoint: drone-photographable = one-pass; pure-paint = compositing.
  - **3 neutral-named background registers** (artist name OUT of system vocabulary; reference artist = unnamed private `--sref`): R1 Color-Block Terrain · R2 Geometric Landform · R3 Chromatic Wave (validated); deferred Pure Color-Field (2-step, manual). Separate **explore lane** (`--c 15–30`).
  - **Prompt-length ~40-word cap RETIRED** — V8.1 honors detailed prompts; lever = kernel + real-phenomenon naming + sref, not word count (LESSONS updated).
  - **Automation north-star** recorded in `automation/`: full auto (theme/prompt-gen → Claude-in-Chrome → IG); user = creative director; one-pass only (compositing manual — V8.1 Editor=V6.1, no official API, unofficial=ToS); **home base deferred** (emerges from `--p` training); consistency = `--p` + image-sref + post-grade LUT.
  - Process: 3-agent audit (style-definition / prompting / cross-doc legacy sweep) → synthesized + authored → **3-agent Round-2 cross-review (converged 0 CRITICAL/HIGH; minor fixes applied)** → committed `ef27769`. Rewrote `style-definition.md` + `prompting.md`; added GLOBAL-realism + bridge + reproducibility-ranking + API-state pointer to the V8.1 research doc; fixed `prompts/README` + `moodboards/README` V7 templates; neutralized giz-as-identity in `reference-accounts`/`README`; resolved prior entry hash `PENDING_COMMIT`→`6b0efbe`.

2026-06-21 [Claude:research] **🟢 V8.1 research doc (web-researched + cross-reviewed) + prompt-length fix — PASS / committed `6b0efbe`**
  - Created `docs/research/2026-06-21_midjourney-v8.1-current.md` (current MJ SSoT). Classified the V7 bundle as **ARCHIVE** (banner + README split: latest vs archive).
  - Built from a 4-agent web-research pass, then a **4-agent web-verified cross-review** (A/C/D/E). Corrections applied: `--sv 4` retained (not removed), `--iw` **0–3** (not 0–2), `--sw` color-band lowered to ~150–250 (300+ experimental), **moodboard untunable by `--sw`/`--sv`**, added **`--hd` (HD-by-default)** + `--exp` + **`/tune` Style Tuner + trained Personalization** (the consistency path for our goal) + `--no` syntax & grammar promoted from archive. **Thesis validated**: two-channel (raw+low-`--s` = realism / sref = color); the moodboard-only route was the wrong tool.
  - Also hardened `docs/style/prompting.md` to a **~40-word target** + added a LESSONS entry (repeated under-delivery on prompt length).
  - Open: relocate the mis-filed `2026-05-11_.../07_future-automation-option.md` out of `docs/research/`.

2026-06-21 [Claude:docs] **🟢 consolidate docs/style 7→4 + apply 2-dial corrections — PASS / committed `da4244f`**
  - Folded `prompting-guide` + `prompt-skill-spec`(recipe) + `idea-bank` → new `docs/style/prompting.md` (craft) with the **corrected recipe** (heroless = first-class, reject = idea-less, `--iw` not `--oref`, element-type defined, single-lane override, soft-painterly register, figure-on-animal hero).
  - Rewrote `style-definition.md` (SSoT) with the **2-dial model** + giz natural-world direction + color/finish corrections + decision log; absorbed world-views' decided direction + future-lane names.
  - **Removed (content folded first; checkpoint `f5d0423` + git history)**: `sref-sweep`, `world-views`, `prompt-skill-spec`, `prompting-guide`, `prompts/idea-bank`. Fixed wikilinks/paths in `reference-accounts` + READMEs.
  - `docs/style/` now: `style-definition` / `prompting` / `reference-accounts` / `README`.

2026-06-21 [Claude:style] **🟢 prompt-model re-verified (2-dial) + doc-consolidation plan — PASS / committed `f5d0423`**
  - Drafted `docs/style/prompt-skill-spec.md` (recipe). Ran multi-agent `/verify` (Full A–E), then a RE-RUN scoped to the user's curated **11-image giz-only** set at `~/Desktop/reference-blend-v1/` (Mariano dropped + swaps).
  - **Model corrected → 2 dials**: subject-dominance × a **3-stop render register** (flat-graphic / soft-painterly / crisp-photoreal), register **NOT derived from subject** (falsified in-set by the sheep / baobab / ibis). Heroless landscapes are **first-class** (~5/11); the real reject is **idea-less**, not heroless. Add **figure-on-animal composite hero** (G01). Demote non-core natural registers (cosmic/sea/grain) to **future scope**. Color-unity is **better** in the giz-only set. `--oref` invalid on V8.1 → use `--iw`; preset table missing; element-type undefined. **Skill DEFERRED** (validate via Phase 1 first; later a thin sampler-only skill).
  - Next: consolidate `docs/style/` **7 → 4** (style-definition SSoT + prompting craft + reference-accounts + README; fold sref-sweep/world-views/prompt-skill-spec/idea-bank, then remove sources — content moved first, no loss) and apply the 2-dial corrections. This entry is the **checkpoint before the restructure**.

2026-06-21 [Claude:style] **🟢 Phase 1: blend-v1 moodboard built & verified; world-views + prompt convention — PASS / committed `17e3466`**
  - MJ plan renewed. Built `blend-v1` moodboard (12 curated refs from `references/`, user-uploaded via browser; `file_upload` tool blocked on session-shared-files, so curated set staged to `~/Desktop/reference-blend-v1/` for manual add). Personalization = blend-v1 only (Global V7/V8 Profile + other moodboards deselected). Verified the blend applies and renders coherently (not muddy).
  - Creative direction explored: `docs/style/world-views.md` (10 world-view candidates), `prompts/idea-bank.md` (conceptual moves). Axis B leaning surreal fine-art × figure × nature/cosmos (not locked).
  - Validated detailed-prompt reproduction (giz tennis court): learned **stylize↓ = fidelity↑**, **specificity narrows range**. Created `docs/style/prompting-guide.md`; adopted **project prompt convention = detailed/specific** (independent agent cross-review → PROCEED WITH CHANGES, applied; research bundle left frozen, sref-sweep probes unchanged).
  - Boundary: no style locked yet (`style-definition.md` still `draft`); no automation.

2026-06-20 [Claude:docs] **🟢 doc cleanup: de-template contracts + frontmatter convention — PASS / committed `dd7c6f4`**
  - Removed legacy "Template-Specific Rules" from `CLAUDE.md` + `AGENTS.md` (project is no longer a template); relocated Project Context to a top-level section + added real Source-of-truth / Verification notes (mirrored in both).
  - Added a minimal frontmatter convention (`docs/CONVENTIONS.md`, no linter) and applied `updated/status/type` frontmatter to `docs/**` + folder READMEs. Removed a stale `mobileeditingclub` reference from `style-definition.md`.
  - Refreshed this PROGRESS (Current State, Roadmap, filled the Approach / Context sections).

2026-06-20 [Claude:style] **🟢 reference accounts analyzed + blend direction + reference pool imported — PASS / committed `bb8bc37`**
  - Confirmed & analyzed reference accounts `giz.akdag` (crisp editorial) + `marianopeccinetti` (retro collage); recorded the two-axis framing (A: taste / B: subject) and the **blend** direction in `docs/style/{reference-accounts,style-definition}.md`.
  - Imported 30 manually-downloaded media into `references/` (18 giz / 7 mariano / 5 unattributed video) via `scripts/import_references.py` + `references/manifest.jsonl` (sha256/provenance). Binaries gitignored; **self-contained — no marketing-ops reuse** (investigated its capture tool but rejected reuse to avoid cross-project contamination).
  - Boundary: no generation yet (MJ plan pending). Next: capture giz SREF codes (Phase 0 remainder); Phase 1 diverge needs the plan.

2026-06-18 [Claude:style] **🟢 sref sweep worksheet — PASS / committed `b08eeff`**
  - Created `docs/style/sref-sweep.md`: divergence-stage worksheet to find a personal style by holding the prompt constant and varying `--sref`. Includes Personalization-control pre-flight, neutral probe prompts per lane, candidate-code table, sweep command patterns, evaluation log, and convergence step.
  - Cross-refs research files 03/05/06. Next: user collects candidate sref codes and runs the sweep.

2026-06-18 [Claude:setup] **🟢 project scaffold + structure + research import — PASS / committed `b08eeff`**
  - Expanded `00_template` (CLAUDE.md / AGENTS.md / .claude / .gitignore); filled Project name + adoption-history + Project Context (Midjourney explore → style → IG automation).
  - Created folders with self-documenting READMEs: `docs/research`, `docs/style`, `prompts`, `moodboards`, `outputs/candidates`, `automation`. Added outputs binary ignores to `.gitignore`.
  - Copied 8-file Midjourney V7 best-practices bundle from `marketing-ops` → `docs/research/2026-05-11_midjourney-prompt-best-practices/`.
  - Verification: `find` confirms tree; structure approved by user. Next: style-establishment phase.

## Key Decisions
- Folder structure mirrors the 3 project phases (explore → style → automate): `docs/research` (external ref), `docs/style` (own-style SSoT), `prompts`, `moodboards`, `outputs`, `automation`.
- `docs/style/` is the SSoT for the project's own visual style; `docs/research/` is read-only external reference (not SSoT).
- outputs binaries git-ignored, metadata sidecars tracked — preserves provenance without bloating the repo.

## Approach for Next Session
Run a Phase-1 **test set per register** (R1/R2/R3) using the kernel + a single bold `--sref` + real-phenomenon background naming; confirm one-pass abstract-feel + photoreal figure holds → promote `style-definition.md` `draft → active`. Then lock per-register base templates in `prompts/`. Track the user's selections (they reveal the home-base register mix + train `--p`). Defer compositing/home-base until the one-pass core is locked. (Formalization cross-reviewed + committed `ef27769`; next concrete step = the R1/R2 test set.)

## Context (Don't Lose)
- **Reference downloads**: scripted IG scraping (gallery-dl, yt-dlp) is blocked by IG auth + Chrome cookie decryption; browser URL-harvest is blocked by the harness redacting signed CDN URLs. Working method = user manually downloads → `scripts/import_references.py` imports into `references/` with a manifest. Self-contained (no marketing-ops reuse, by user request).
- **giz.akdag SREF codes**: not harvested — highlight slides are videos (unreadable) and the codes look like a paid product on her Contra. Color route = a self-uploaded bold `--sref` image, not a moodboard.
- **Style model (formalized 2026-06-23)**: foreground photoreal invariant (kernel) + color via single bold `--sref` + 3 background registers R1/R2/R3 + the bridge. The artist name is out of the system vocabulary (unnamed private `--sref`). SSoT: `docs/style/style-definition.md`.
- **Reference-image upload gotcha**: MJ image upload (moodboard or `--sref`) needs the browser; the `file_upload` tool rejects project/temp paths ("session-shared files only"), so the user uploads manually (stage curated files to `~/Desktop/` for an easy drag-add).
- **Prompt convention**: detailed/specific, but **length is not the lever** (no ~40-word cap) — the levers are the kernel + naming the real subject/phenomenon + the `--sref`. SSoT: `docs/style/prompting.md`.
- **Standalone menu — PFP/icon line-art (NOT core style, DO NOT generalize)**: profile-picture deliverables (matte color sphere → flat color circle → minimal line-art circle with white knockout) are a SEPARATE track. Their tuned params are LOCAL to this request and must NOT leak into the core recipe: `--style raw` dropped, `painting/illustration` removed from `--no`, `--sw ~100`, flat 2D line-art, white-knockout. Core style (`docs/style/prompting.md`, `style-definition.md`) stays the photoreal-color-wave recipe — keep them distinct. **Formalized: `prompts/icon-pfp/README.md`** (adopted V1 = no-theme abstract wave circle, flat-graphic, white only outside). Finding: the **`--sref` pick dominates** the output (clean bands vs noisy glitch) — record the exact sref per adopted icon.
