---
updated: 2026-07-02
status: active
type: index
---

# docs/style

**SSoT for the project's own visual style** — the project's own decisions (distinct from `docs/research/`,
which is external reference).

- **`style-definition.md`** — the SSoT: the **render rule** (realism scoped to subject + anchors), the **base
  (Flowing Color Waves) + sibling registers** + the **bridge**, the surreal natural world, the color channel
  (`--sref`), the **reward gate**, the automation north-star, and the decision / verification log.
- **`prompting.md`** — the craft: the realism kernel + color channel + the 3 registers (with examples) +
  the bridge + the §9 lane family (explore / Remix-Fidelity / sref-only / motion-dissolve) + idea engine +
  anti-patterns + idea seeds.
- **`reference-accounts.md`** — the reference artists analysis + the imported reference pool (inspiration
  only; the artist name stays out of the style vocabulary — an unnamed private `--sref` image).
- **`exploration-map.md`** — the live subject-coverage plan for the IG base runway: areas A–G with status,
  the 7 wave modes, the active lanes (engulfment / Escher / soccer motion-dissolve), the lever palette, and
  the reality-defying lane (engulfment promoted).

Naming: kebab-case `.md`; frontmatter per `../CONVENTIONS.md`; record style decisions as dated log entries
(don't overwrite history).
